version: '3.9'
services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: aims-sqlserver
    environment:
      ACCEPT_EULA: 'Y'
      SA_PASSWORD: 'Del12345!'
    ports:
      - '1433:1433'
    volumes:
      - sqlserver-data:/var/opt/mssql
    healthcheck:
      test: ["CMD", "/opt/mssql-tools18/bin/sqlcmd", "-S", "localhost", "-U", "sa", "-P", "Del12345!", "-Q", "SELECT 1", "-C"]
      interval: 10s
      retries: 10
      timeout: 10s
      start_period: 30s

  webfrontend:
    build:
      context: .
      dockerfile: src/AIMS.WebFrontend/Dockerfile
    container_name: aims-webfrontend
    depends_on:
      sqlserver:
        condition: service_started
    ports:
      - '8081:8081'
    environment:
      ASPNETCORE_URLS: 'http://+:8081'
      ASPNETCORE_ENVIRONMENT: 'Development'
      ConnectionStrings__DefaultConnection: 'Server=sqlserver;Database=aimsdbv2;User Id=sa;Password=Del12345!;Trusted_Connection=False;MultipleActiveResultSets=True;TrustServerCertificate=True'
    restart: unless-stopped

volumes:
  sqlserver-data:
